package ch.fhnw.cpib.platform.scanner.tokens.sentinel;

import ch.fhnw.cpib.platform.scanner.terminal.Terminal;
import ch.fhnw.cpib.platform.scanner.tokens.Token;

public class SentinelToken extends Token {

    public SentinelToken(Terminal terminal) {
        super(terminal);
    }
}
