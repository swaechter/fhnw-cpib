program Factorial(in n:int32, out f:int64)
global
  fun fact32(n:int32) returns var fact:int32
  local
    var i:int32
  do
    fact init := 1;
    i    init := 2;
    while i <= n do
      fact := fact * i;
      i    := i + 1
    endwhile
  endfun;

  fun fact64(n:int32) returns var fact:int64
  local
    var i:int32
  do
    fact init := 1;
    i    init := 2;
    while i <= n do
      fact := fact * i;
      i    := i + 1
    endwhile
  endfun;

  fun fact64(n:int32) returns var fact:int64
  local
    var i:int32
  do
    fact init := 1;
    i    init := 2;
    while i <= n do
      fact := fact * i;
      i    := i + 1
    endwhile
  endfun
do
  f init := fact64(n)
endprogram
